import { endUser } from 'bothunt/helpers/end-user';
import { module, test } from 'qunit';

module('Unit | Helper | end user');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = endUser([42]);
  assert.ok(result);
});
